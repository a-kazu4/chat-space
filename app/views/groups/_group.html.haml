.group
  = link_to group_messages_path(group), class: 'group__box' do
    .group__name
      = group.name
    .group__message
      - if group.messages.empty?
        まだメッセージがありません。
      - else
        = group.messages.order('created_at DESC').first.try(:body)
